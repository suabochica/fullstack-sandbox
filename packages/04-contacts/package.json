
{
  "name": "contacts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev:client": "pnpm --filter contacts-frontend dev",
    "dev:server": "pnpm --filter contacts-server watch"
  }
}
