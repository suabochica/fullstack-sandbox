{
  "name": "contacts-server",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "watch": "node --watch server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "body-parser": "^1.17.1",
    "clone": "^2.1.1",
    "cors": "^2.8.3",
    "express": "^4.15.2"
  }
}
